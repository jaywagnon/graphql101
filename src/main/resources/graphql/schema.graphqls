schema {
    query: Query
}

type Author {
    name: String!
}

type Book {
    name: String!
}

type Query {
    "Simply returns 'true'.  A no-op query to satisfy the minimum requirements for a GraphQL schema."
    healthCheck: Boolean!

    getAuthors: [Author!]!

    getBooks: [Book!]!
}
